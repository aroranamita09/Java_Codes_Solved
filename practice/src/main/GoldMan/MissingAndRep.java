package main.GoldMan;

public class MissingAndRep {
    //Create a temp array of size n
    //if temp[arr[i]==1 then print arr[i] as repeating and if temp[arr[i]]==0 that is missing
    void printRepeatedElement(int[] arr,int size){
        int i;
        System.out.print("The repeating element is");
        for(i=0;i<size;i++){
            int abs_val=Math.abs(arr[i]);
        }
    }
 }
